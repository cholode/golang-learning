

### overlaps 源码

```go

func overlaps[E any](a, b []E) bool {//判断两个切片是否共享了部分内存
	if len(a) == 0 || len(b) == 0 {//如果存在切片长度为0
		return false
	}
	elemSize := unsafe.Sizeof(a[0])//计算单个元素大小，any大小不固定
	if elemSize == 0 {//是struct{}直接返回
		return false
	}
	//通过占用范围判断是否有重叠部分
	return uintptr(unsafe.Pointer(&a[0])) <= uintptr(unsafe.Pointer(&b[len(b)-1]))+(elemSize-1) &&
		uintptr(unsafe.Pointer(&b[0])) <= uintptr(unsafe.Pointer(&a[len(a)-1]))+(elemSize-1)
}


```

### rotateRight 源码

```go
func rotateLeft[E any](s []E, r int) {
	Reverse(s[:r])
	Reverse(s[r:])
	Reverse(s)
}
```
**第一步：翻转前 r 个元素**

```go
Reverse(s[:r]) // 翻转前 2 个
```
执行前：[1, 2, 3, 4, 5, 6]
执行后：[2, 1, 3, 4, 5, 6]
第二步：翻转 r 之后的剩余元素

```go
Reverse(s[r:]) // 翻转索引 2 到末尾的所有元素
```
执行前：[2, 1, 3, 4, 5, 6]
执行后：[2, 1, 6, 5, 4, 3]
第三步：翻转整个切片

```go
Reverse(s) // 翻转全部元素
```
执行前：[2, 1, 6, 5, 4, 3]
执行后：[3, 4, 5, 6, 1, 2]
向右平移r位


### Insert 源码
```go
func Insert[S ~[]E, E any](s S, i int, v ...E) S {
	_ = s[i:] // bounds check

	m := len(v)
	if m == 0 {
		return s
	}
	n := len(s)
	if i == n {//如果是接在末尾
		return append(s, v...)
	}
    
	if n+m > cap(s) {//如果超过容量
		s2 := append(s[:i], make(S, n+m-i)...)//直接开新的容量
		copy(s2[i:], v)//覆盖后面的v
		copy(s2[i+m:], s[i:])//然后再把末尾盖上去
		return s2//返回新的切片
	}
	s = s[:n+m]//扩大长度


	if !overlaps(v, s[i+m:]) {//如果没有重复

		copy(s[i+m:], s[i:])//先把被挤的复制到后面，如果重叠的话会导致插入切片被这一步改变

		copy(s[i:], v)//再把要插入了复制到中间

		return s
	}

	copy(s[n:], v)//复制到不会受影响的右边

	rotateRight(s[i:], m)//平移m位
	return s
}
```





